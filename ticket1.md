# Lighthouse Score Performance Optimization

The peformance of the page is bad, to correct this we can resize the image, and do the components load more quickly

The accessibitility was not bad, but we can improve avoinding use text with light colors on light backgorunds

The SEO, can be more improved to make the page better ranked on search plataforms, we can do this using the HTML more semantic, and do the configuration of head and header.